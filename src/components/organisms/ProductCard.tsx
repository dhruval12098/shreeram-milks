import { memo } from 'react';
import { Pressable, View } from 'react-native';
import { Image } from 'expo-image';
import { ThemedText } from '../atoms/ThemedText';
import type { Product } from '../../types/models';
import { useTheme } from '../../theme';
interface ProductCardProps { isAdded?: boolean; onAdd: (productId: string) => void; onPress: (productId: string) => void; product: Product; }
export const ProductCard = memo(function ProductCard({ isAdded = false, onAdd, onPress, product }: ProductCardProps) { const theme = useTheme(); return <Pressable accessibilityRole="button" accessibilityLabel={product.name} onPress={() => onPress(product.id)} style={{ flex: 1, overflow: 'hidden', borderRadius: theme.radii.lg, borderWidth: theme.borderWidths.hairline, borderColor: theme.colors.colorBorder, backgroundColor: theme.colors.colorSurface }}><Image source={{ uri: product.imageUrl ?? undefined }} contentFit="cover" transition={180} style={{ height: 156, backgroundColor: theme.colors.colorPrimaryTint }} /><View style={{ gap: theme.spacing.xs, padding: theme.spacing.sm }}><ThemedText variant="bodySmall" weight="semibold" numberOfLines={1}>{product.name}</ThemedText><ThemedText variant="caption" numberOfLines={1} style={{ color: theme.colors.colorTextSecondary }}>{product.unit}</ThemedText><View style={{ flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginTop: theme.spacing.xs }}><ThemedText variant="bodySmall" weight="semibold">{`₹${product.price}`}</ThemedText><Pressable accessibilityRole="button" accessibilityLabel={`${isAdded ? 'Added' : 'Add'} ${product.name}`} onPress={() => onAdd(product.id)} style={{ height: 32, alignItems: 'center', justifyContent: 'center', borderRadius: theme.radii.pill, paddingHorizontal: theme.spacing.sm, backgroundColor: isAdded ? theme.colors.colorPrimaryPressed : theme.colors.colorPrimary }}><ThemedText variant="caption" weight="semibold" style={{ color: theme.colors.colorTextInverse, lineHeight: 14 }}>{isAdded ? 'ADDED' : '+ ADD'}</ThemedText></Pressable></View></View></Pressable>; });
